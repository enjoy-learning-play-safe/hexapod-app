import { solveActuator } from './solveActuator';

describe('#solveActuator', () => {
  test('should return expected 1d tensor', () => {
    expect(true).toBeTruthy();
    // expect(solveActuator().toEqual([]));
  });
});
