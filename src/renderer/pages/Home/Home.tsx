import { Flex } from '@chakra-ui/react';
import React from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface Props {}

const Home = (props: Props) => {
  return <Flex p={4}>i am Home.tsx!</Flex>;
};

export default Home;
